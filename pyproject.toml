[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "content-ops"
version = "0.1.0"
description = "Local LinkedIn content operations"
requires-python = ">=3.12"
